1、适配移动端 竖屏的时候也要丝滑使用，你可以模拟竖屏挨个测试页面，并修改样式
2、如果是ppt、doc、xls等文件的预览，我希望能够撑满整个页面，现在预览左右都有白边，需要修复。
3、预览的时候，如果不是图片，下方类似dock栏的预览图需要显示文件的名称。
4、搜索有两个方式一个顶部的搜索，为全文搜索文件名称。一个是当前文件夹的搜索，只搜索当前文件夹。
5、文件可以生成一个用不过期的url链接，也就是通过oss直接访问，这样方便我直接使用url。
6、开放一个匿名用户上传文件的功能，左侧增加一个菜单，可以生成一个分享页面，匿名用户上传文件后，我可以看到，并且可以选择将它保存到我的文件里面，支持过期，过期后不能再打开页面，且主动从oss清理。
7、分享的链接，如果是图片、文档、视频、md支持预览。
8、文件夹有文件的不支持删除